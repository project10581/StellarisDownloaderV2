<Window x:Class="StellarisDownloader.App.DownloadQueueWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Download.Title}"
        Width="920"
        Height="720"
        MinWidth="720"
        MinHeight="540"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="150" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource Download.InputLabel}"
                   Margin="0,0,0,5" />
        <TextBox Grid.Row="1"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Padding="8"
                 TextWrapping="NoWrap" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,8">
            <Button Content="{DynamicResource Download.Add}"
                    Command="{Binding EnqueueCommand}"
                    Padding="12,6"
                    Margin="0,0,8,0" />
            <Button Content="{DynamicResource Download.RemoveSelected}"
                    Click="RemoveSelected_Click"
                    Padding="12,6"
                    Margin="0,0,8,0" />
            <Button Content="{DynamicResource Download.Clear}"
                    Click="ClearQueue_Click"
                    Padding="12,6" />
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="{DynamicResource Download.Queue}">
                <ListBox x:Name="QueueList"
                         ItemsSource="{Binding Items}"
                         SelectionMode="Extended"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayTitle}"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding WorkshopId}"
                                               Foreground="Gray" />
                                    <TextBlock Text="{Binding Error}"
                                               Foreground="Firebrick"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Status}"
                                           Margin="12,0,0,0"
                                           VerticalAlignment="Top" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>

            <GroupBox Grid.Column="2" Header="{DynamicResource Download.InvalidInputs}">
                <ListBox ItemsSource="{Binding InvalidInputs}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="4">
                                <TextBlock>
                                    <Run Text="#" />
                                    <Run Text="{Binding LineNumber}" />
                                    <Run Text=": " />
                                    <Run Text="{Binding Input}" />
                                </TextBlock>
                                <TextBlock Text="{Binding Error}"
                                           Foreground="Firebrick"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
        </Grid>

        <Grid Grid.Row="4" Margin="0,10,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{DynamicResource Download.ProgressLabel}" />
                <TextBlock Text="{Binding ProgressCompleted}" Margin="4,0,0,0" />
                <TextBlock Text="/" Margin="3,0" />
                <TextBlock Text="{Binding ProgressTotal}" />
                <TextBlock Text="{Binding CurrentStage}" Margin="16,0,0,0" />
                <TextBlock Text="{Binding CurrentWorkshopId}" Margin="8,0,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="{DynamicResource Download.Start}"
                        Command="{Binding StartCommand}"
                        Padding="12,6"
                        Margin="0,0,8,0" />
                <Button Content="{DynamicResource Download.Cancel}"
                        Command="{Binding CancelCommand}"
                        Padding="12,6"
                        Margin="0,0,8,0" />
                <Button Content="{DynamicResource Download.RetryFailed}"
                        Command="{Binding RetryFailedCommand}"
                        Padding="12,6" />
            </StackPanel>
        </Grid>

        <GroupBox Grid.Row="5" Header="{DynamicResource Download.Log}">
            <ListBox ItemsSource="{Binding Logs}"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </GroupBox>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="{DynamicResource Download.SucceededLabel}" />
            <TextBlock Text="{Binding SucceededCount}" Margin="4,0,16,0" />
            <TextBlock Text="{DynamicResource Download.FailedLabel}" />
            <TextBlock Text="{Binding FailedCount}" Margin="4,0,16,0" />
            <TextBlock Text="{DynamicResource Download.CancelledLabel}" />
            <TextBlock Text="{Binding CancelledCount}" Margin="4,0,16,0" />
            <TextBlock Text="{Binding LastError}"
                       Foreground="Firebrick"
                       TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>
